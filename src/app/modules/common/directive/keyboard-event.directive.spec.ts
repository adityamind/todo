import { KeyboardEventDirective } from './keyboard-event.directive';

describe('KeyboardEventDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardEventDirective();
    expect(directive).toBeTruthy();
  });
});
